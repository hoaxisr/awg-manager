diff --git a/server/config.go b/server/config.go
index 89ee8a4..3fc2cb2 100644
--- a/server/config.go
+++ b/server/config.go
@@ -20,6 +20,8 @@ type ServerConfig struct {
 	HandshakeTimeout time.Duration
 	AdminAddr        string
 	MaxDTLSPerDevice int
+	NoNAT            bool
+	NatIface         string
 }
 
 var (
@@ -33,6 +35,8 @@ var (
 	flagHandshake   *time.Duration
 	flagAdminAddr   *string
 	flagMaxDTLSDev  *int
+	flagNoNAT       *bool
+	flagNatIface    *string
 )
 
 func initServerFlags() {
@@ -46,6 +50,8 @@ func initServerFlags() {
 		flagHandshake = flag.Duration("handshake-timeout", 30*time.Second, "таймаут DTLS handshake")
 		flagAdminAddr = flag.String("admin-addr", "127.0.0.1:2861", "HTTP admin (localhost): /health, POST /admin/reload")
 		flagMaxDTLSDev = flag.Int("max-dtls-per-device", 0, "макс. параллельных GETCONF на device_id (0 = без лимита)")
+		flagNoNAT = flag.Bool("no-nat", false, "skip iptables/nft NAT setup (NAT handled by router/awg-manager)")
+		flagNatIface = flag.String("nat-if", "", "egress interface for MASQUERADE (empty = auto-detect WAN)")
 		flag.Parse()
 	})
 }
@@ -62,6 +68,8 @@ func baseServerConfigFromFlags() ServerConfig {
 		HandshakeTimeout: *flagHandshake,
 		AdminAddr:        *flagAdminAddr,
 		MaxDTLSPerDevice: *flagMaxDTLSDev,
+		NoNAT:            *flagNoNAT,
+		NatIface:         strings.TrimSpace(*flagNatIface),
 	}
 }
 
diff --git a/server/server.go b/server/server.go
index 28d68a1..22e9ac7 100644
--- a/server/server.go
+++ b/server/server.go
@@ -537,7 +537,7 @@ func runServerOnce(ctx context.Context, cfg ServerConfig) {
 
 	enableBBR()
 
-	wgDev, err := startUserspaceWG(keys, cfg.WgPort)
+	wgDev, err := startUserspaceWG(keys, cfg.WgPort, cfg)
 	if err != nil {
 		log.Fatalf("[WG] Запуск: %v", err)
 	}
@@ -550,6 +550,7 @@ func runServerOnce(ctx context.Context, cfg ServerConfig) {
 	syncPersistedPeersToWG(wgDev)
 	syncAllSpeedLimits()
 	defer func() {
+		cleanupManagedNAT(wgIfaceName)
 		resetTcOnIface(wgIfaceName)
 		wgDev.Close()
 		runCmdSilent("ip", "link", "del", wgIfaceName)
diff --git a/server/server_wg.go b/server/server_wg.go
index b2b9b2b..e74d0b5 100644
--- a/server/server_wg.go
+++ b/server/server_wg.go
@@ -370,14 +370,22 @@ generate:
 
 // ==================== NAT ====================
 
-func setupFullConeNAT(wgIface string) error {
+func setupFullConeNAT(wgIface string, noNAT bool, natIface string) error {
 	log.Println("[NAT] ══════════════════════════════════════")
+	if noNAT {
+		log.Println("[NAT] пропуск (-no-nat): iptables/nft/ip_forward не трогаем")
+		log.Println("[NAT] ══════════════════════════════════════")
+		return nil
+	}
 
 	os.WriteFile("/proc/sys/net/ipv4/ip_forward", []byte("1"), 0644)
 
 	setupForwardRules(wgIface)
 	syncVPNLocalServices(wgIface)
-	extIface := getDefaultInterface()
+	extIface := strings.TrimSpace(natIface)
+	if extIface == "" {
+		extIface = getDefaultInterface()
+	}
 	log.Printf("[NAT] Внешний: %s", extIface)
 
 	switch {
@@ -492,9 +500,28 @@ func setupForwardRules(wgIface string) {
 	}
 }
 
+func cleanupManagedNAT(wgIface string) {
+	log.Println("[NAT] cleanup WDTT_MANAGED rules")
+	if commandExists("iptables") {
+		for i := 0; i < 5; i++ {
+			exec.Command("iptables", "-t", "nat", "-D", "POSTROUTING", "-s", wgServerCIDR, "-m", "comment", "--comment", "WDTT_MANAGED", "-j", "MASQUERADE").Run()
+			exec.Command("iptables", "-D", "FORWARD", "-i", wgIface, "-m", "comment", "--comment", "WDTT_MANAGED", "-j", "ACCEPT").Run()
+			exec.Command("iptables", "-D", "FORWARD", "-o", wgIface, "-m", "comment", "--comment", "WDTT_MANAGED", "-j", "ACCEPT").Run()
+		}
+		vpnLocalPortsMu.Lock()
+		for _, port := range vpnLocalPortsApplied {
+			removeVPNLocalPortRule(wgIface, port)
+		}
+		vpnLocalPortsApplied = nil
+		vpnLocalPortsMu.Unlock()
+	}
+	exec.Command("nft", "delete", "table", "ip", "wdtt").Run()
+	exec.Command("nft", "delete", "table", "inet", "wdtt").Run()
+}
+
 // ==================== WireGuard ====================
 
-func startUserspaceWG(keys *wgKeys, wgPort int) (*device.Device, error) {
+func startUserspaceWG(keys *wgKeys, wgPort int, cfg ServerConfig) (*device.Device, error) {
 	runCmdSilent("ip", "link", "del", wgIfaceName)
 	time.Sleep(100 * time.Millisecond)
 
@@ -540,7 +567,7 @@ func startUserspaceWG(keys *wgKeys, wgPort int) (*device.Device, error) {
 		return nil, err
 	}
 
-	if err := setupFullConeNAT(ifaceName); err != nil {
+	if err := setupFullConeNAT(ifaceName, cfg.NoNAT, cfg.NatIface); err != nil {
 		dev.Close()
 		return nil, err
 	}
