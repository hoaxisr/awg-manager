diff --git a/server/config.go b/server/config.go
index 3fc2cb2..c4d5e6f 100644
--- a/server/config.go
+++ b/server/config.go
@@ -20,6 +20,7 @@ type ServerConfig struct {
 	HandshakeTimeout time.Duration
 	AdminAddr        string
 	MaxDTLSPerDevice int
+	WGIface          string
 	NoNAT            bool
 	NatIface         string
 }
@@ -35,6 +36,7 @@ var (
 	flagHandshake   *time.Duration
 	flagAdminAddr   *string
 	flagMaxDTLSDev  *int
+	flagWGIface     *string
 	flagNoNAT       *bool
 	flagNatIface    *string
 )
@@ -50,6 +52,7 @@ func initServerFlags() {
 		flagAdminAddr = flag.String("admin-addr", "127.0.0.1:2861", "HTTP admin (localhost): /health, POST /admin/reload")
 		flagMaxDTLSDev = flag.Int("max-dtls-per-device", 0, "макс. параллельных GETCONF на device_id (0 = без лимита)")
 		flagNoNAT = flag.Bool("no-nat", false, "skip iptables/nft NAT setup (NAT handled by router/awg-manager)")
+		flagWGIface = flag.String("wg-iface", "wdtt0", "имя userspace WireGuard-интерфейса (opkgtunN для Keenetic NDMS)")
 		flagNatIface = flag.String("nat-if", "", "egress interface for MASQUERADE (empty = auto-detect WAN)")
 		flag.Parse()
 	})
@@ -68,6 +71,7 @@ func baseServerConfigFromFlags() ServerConfig {
 		AdminAddr:        *flagAdminAddr,
 		MaxDTLSPerDevice: *flagMaxDTLSDev,
 		NoNAT:            *flagNoNAT,
+		WGIface:          strings.TrimSpace(*flagWGIface),
 		NatIface:         strings.TrimSpace(*flagNatIface),
 	}
 }
diff --git a/server/server.go b/server/server.go
index 22e9ac7..d5e6f7a 100644
--- a/server/server.go
+++ b/server/server.go
@@ -24,7 +24,7 @@ import (
 )
 
 const (
-	wgIfaceName           = "wdtt0"
+	defaultWGIfaceName    = "wdtt0"
 	wgServerAddr          = "10.66.66.1"
 	wgServerCIDR          = wgServerAddr + "/24"
 	defaultInternalWGPort = 56001
@@ -38,6 +38,8 @@ const (
 	defaultStatsIntervalSec = 2
 )
 
+var wgIfaceName = defaultWGIfaceName
+
 var (
 	wgKeepaliveSec   = keepalive
 	statsIntervalSec = defaultStatsIntervalSec
@@ -537,6 +539,10 @@ func runServerOnce(ctx context.Context, cfg ServerConfig) {
 
 	enableBBR()
 
+	if n := strings.TrimSpace(cfg.WGIface); n != "" {
+		wgIfaceName = n
+	}
+
 	wgDev, err := startUserspaceWG(keys, cfg.WgPort, cfg)
 	if err != nil {
 		log.Fatalf("[WG] Запуск: %v", err)
