diff --git a/server/config.go b/server/config.go
--- a/server/config.go
+++ b/server/config.go
@@ -23,6 +23,7 @@ type ServerConfig struct {
 	WGIface          string
 	NoNAT            bool
 	NatIface         string
+	RawListen        string
 }
 
 var (
@@ -39,6 +40,7 @@ var (
 	flagWGIface     *string
 	flagNoNAT       *bool
 	flagNatIface    *string
+	flagListenRaw   *string
 )
 
 func initServerFlags() {
@@ -55,6 +57,7 @@ func initServerFlags() {
 		flagNoNAT = flag.Bool("no-nat", false, "skip iptables/nft NAT setup (NAT handled by router/awg-manager)")
 		flagWGIface = flag.String("wg-iface", "wdtt0", "имя userspace WireGuard-интерфейса (opkgtunN для Keenetic NDMS)")
 		flagNatIface = flag.String("nat-if", "", "egress interface for MASQUERADE (empty = auto-detect WAN)")
+		flagListenRaw = flag.String("listen-raw", "", "Raw UDP (без DTLS/WG), например 0.0.0.0:56003")
 		flag.Parse()
 	})
 }
@@ -74,6 +77,7 @@ func baseServerConfigFromFlags() ServerConfig {
 		NoNAT:            *flagNoNAT,
 		WGIface:          strings.TrimSpace(*flagWGIface),
 		NatIface:         strings.TrimSpace(*flagNatIface),
+		RawListen:        strings.TrimSpace(*flagListenRaw),
 	}
 }
 
diff --git a/server/server.go b/server/server.go
--- a/server/server.go
+++ b/server/server.go
@@ -596,6 +596,14 @@ func runServerOnce(ctx context.Context, cfg ServerConfig) {
 	if serverWrapKeys.Count() == 0 {
 		log.Fatalf("[WRAP] нет активных паролей для WRAP")
 	}
+	if rawListen := strings.TrimSpace(cfg.RawListen); rawListen != "" {
+		go func() {
+			if err := runRawServer(ctx, cfg, rawListen); err != nil && ctx.Err() == nil {
+				log.Fatalf("[RAW] %v", err)
+			}
+		}()
+	}
 
 	wrapListener, err := listenWrapped(addr, serverWrapKeys)
 	if err != nil {
